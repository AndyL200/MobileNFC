import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  darkveilCanvas: {
    width: '100%',
    height: '100%',
    display: 'flex', // React Native defaults to flex, "block" isn't valid
  },
});